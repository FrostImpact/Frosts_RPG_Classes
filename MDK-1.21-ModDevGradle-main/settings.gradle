pluginManagement {
    repositories {
        gradlePluginPortal()

        maven {
            name = "NeoForge"
            url = "https://maven.neoforged.net/"
        }

        maven {
            name = "Sinytra"
            url = "https://mvnrepository.com/artifact/org.sinytra.forgified-fabric-api"
        }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}
